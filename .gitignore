node_modules/

# Build output. Not committed — `prepublishOnly` rebuilds it at publish time,
# and the tarball ships it via package.json "files", so consumers installing
# the published package get a fresh dist without rebuilding on their end.
dist/

# Preview demo build output (vite.config.ts), separate from the library dist.
dist-preview/

# Local env / editor noise
.env
.env.*
*.log
.DS_Store
.vscode/
.idea/
